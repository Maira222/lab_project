---
# Main playbook - Configure frontend and backend servers
- name: Configure backend HTTPD servers
  hosts: backends
  become: true
  gather_facts: true
  roles:
    - backend

- name: Configure frontend Nginx load balancer
  hosts: frontend
  become: true
  gather_facts: true
  roles:
    - frontend
